// Fixture behavior-only script (no content strings) — mirrors spec/03 §4's contract.
document.addEventListener("DOMContentLoaded", () => {
  // intentionally empty; the fixture doesn't need real interactivity for builder tests
});
