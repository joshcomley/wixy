# TODO index — workspace 00021 (wixy)

Task: import ALL of Purdi's Instagram + Facebook before/after posts as Wixy before/after
entities, each with a "Show on site" toggle. Full brief from planner cmd session
`75f673ab-803c-44ea-a863-edc04f1783e9`. Operating contract: implementer-owned dev, TWO-round
FINAL HANDOFF gate (engine PR1 alone, then everything else), then merge + release +
POST-RELEASE DONE REPORT. Blocker path: POST to
`http://127.0.0.1:9321/sessions/75f673ab-803c-44ea-a863-edc04f1783e9/send`.

**Project complete as of 2026-08-09 (round c).** All 6 PRs merged, deployed, live, and
verified. Op decision #243 obsoleted — the operator solved authenticated IG/FB access
himself (fresh Chrome profile, manual login) rather than answering it. That access was then
used to fully complete before/after coverage (58 more entries, round c) and build a full
raw social-media archive at `D:\Servers\Wixy\Storage\projects\ca\social-archive\`. A
first-class Wixy "posts" content type + admin section + daily-sync (operator's own framing,
2026-08-09 chat) is confirmed-deferred future engineering work, not started this round.

## Done

- DONE [w54r03] Engine D1: `visible` item convention in builder (bindings.py, schemas) — builder/bindings.py ATTR_ITEM_HIDDEN + _expand_list, both schemas, tests green
- DONE [s1stlw] Engine D2: `toggle` admin field kind (config.py, ca.json, admin-ui) — AdminFieldKind, sectionPanel.ts renderToggleField+add-flow fix, sectionPanelModel.ts removeItemField, CSS, tests green, bundle built
- DONE [4myclw] Engine D3: editor overlay read-back + structural ops for hidden items (red/green) — contentModel.ts readListValue, listOps.ts add-strips/duplicate-keeps, overlay.ts DOM strip, ghost CSS; red/green regression test proven against reverted code; tests green, bundle built
- DONE [vlgqe7] Engine D4: e2e + docs + decisions + ship PR1 — PR #154 (https://github.com/joshcomley/wixy/pull/154) MERGED 2026-08-09 (merge commit c432cf8fe320650db512ef3980a1fed03ae971ed) after planner FINAL HANDOFF CLEARED (0 critical/0 high, 1 low: "Hidden" chip DOM-order — check on real device in Workstream 4's visual pass). Slots deploy verified: GET /api/version sha == merge commit, slot flipped green->blue.
- DONE [oh5zsa] Workstream 2: scrape+classify+process IG & FB before/after posts, build manifest — agent `ad859cceb1d525fd9` did the bulk scrape/classify/process (crashed on a transient API error, resumed twice via operator's IG/FB login #239→#241; login profile-copy tech dead-ended on Chrome's App-Bound Encryption + CDP default-dir guard, both deliberate anti-automation hardening — raised op-ask #242 for a cookie-export-extension approach instead; #242 auto-closed on a clarifying question rather than a choice, superseded by #243, still open/delayed, not blocking). I (main session) then found+fixed a REAL conflict myself: a dedup sub-agent's report on the 10 candidate pairs disagreed with manifest.json on 6 items — visually re-verified all 6 personally against distinctive markers (mole position, septum piercing, unique necklace, baked-in BEFORE/AFTER banner) vs weak styling-only signals (clinic wrap colour, common earrings): 4 confirmed genuine duplicates and excluded (DY-SqBiCKSk, DXXdiCwCBxz, DaNqV5SI7wh-a/b), DYndLj0oxoi stayed excluded (mole match), DZs3KJKjGHL reversed to import-but-hidden (genuinely ambiguous — no biological marker either way, let Purdi decide herself). Final: 9 confirmed imports (lips×5, cheeks×1, chin×2, eyes×1), all landed in site-repo PR #25. Enumeration completeness still honestly gapped (IG 34/243, FB ~34/unknown) — documented in decisions/00008 in the site repo, pending op decision #243.
- DONE [iuylh3] Workstream 3: Engine PR2 (categories) + site-repo PR (images+content+decisions) — PR #155 (wixy, https://github.com/joshcomley/wixy/pull/155, head 844bb1e5a4bf0413959b2dab5e818e932ed13350) and PR #25 (site repo, https://github.com/joshcomley/cottage-aesthetics-preview/pull/25, head 4b5d2bdb49cf338bc5ab3ba6ec5fa0682d2fda95) both built, pushed, CI fully green. PR #25's CI failed twice first (root-caused, not worked around): a genuinely stale parity baseline unrelated to this PR (fixed via wixy's capture-baseline.yml dispatched against site-repo main), then a real diff — the new hide-empty-filter-button script also correctly hides a pre-existing, permanently-dead "Cheeks" button (intended behavior, not a regression) — fixed by recapturing the baseline from this PR's own branch instead. Both steps + reasoning documented in decisions/00008's "What to watch for". Confirmed `cat` is schema-unenforced, so PR #155 doesn't actually gate PR #25's CI (only the admin dropdown) — both still need merging, just not strictly sequenced.
- DONE [rusa0k] Workstream 4: round-(b) FINAL HANDOFF, merge, publish, live verification, DONE report — planner cleared round-(b) (0 critical/0 high) for both exact SHAs; merged PR #155 (merge commit 6b2dc5fd3135b68a5a910300578bcc81091e9164, Slots deploy verified live via /api/version before proceeding) then PR #25 (merge commit 8062df5d50adfda1c627f270f3d201033aad84ea). Escalated op decision #243 delayed->immediate (bounded 180s wait) per planner's guidance — no answer, stayed open/delayed, proceeded per doctrine. Published: draft.opCount=0 confirmed clean, POST /api/admin/publish -> version 35, sha matches PR #25's merge commit exactly. Live verification: public gallery byte-unchanged (7 sliders+1 tile, zero imported filenames leaked); admin content shows all 17 entities (9 correctly hidden); admin "eyes" category live in the cat dropdown; toggle-show-then-DELETE-draft-discard round trip proven with zero lasting state; editor overlay ghosting confirmed (9/9 items, opacity 0.4, "Hidden" badge) at the correct /admin/preview/gallery.html route after an initial wrong-URL false-negative was caught and fixed; mobile screenshot confirms the "Hidden" chip (the planner's round-a flagged low finding) renders cleanly, no clipping — resolved. Also shipped a small docs-only follow-up PR #156 (todos index) to keep this file itself accurate without touching the cleared candidate SHAs. POST-RELEASE DONE REPORT sent to planner 2026-08-09.
- DONE [round-c] Full-corpus before/after completion + raw archive — operator solved the enumeration gap himself mid-session: opened a fresh, automation-controlled Chrome profile (no prior login to protect) and logged into IG+FB manually, sidestepping both Chrome protections in decisions/00008 entirely (nothing copied, login happens natively in the profile Playwright already controls). Op decision #243 marked obsolete as a result. Used the unblocked access for TWO deliverables the operator asked for in chat: (1) a full raw archive of all 272 reachable posts (233/243 IG incl. reels, 39 FB) — images/video-where-possible/captions/dates — built at `D:\Servers\Wixy\Storage\projects\ca\social-archive\` (wixy's own persistent, non-git, non-deployed project storage), with an honest documented gap (39 reels' video undownloadable — Instagram serves DASH byte-range segments, not a fetchable file; caption/date captured regardless, reels are never before/after candidates anyway); (2) complete before/after coverage — 8 parallel agents classified the 205 posts new since round b, found 63 candidates, deduped via image-hash + direct visual marker verification (both against the existing gallery and internally against each other) down to 58 confirmed new entries. Shipped as cottage-aesthetics-preview PR #26 (merge commit 50b3b8003a3ed8b76235be9daf1c0d8b7253329f), all 58 visible:false per the established convention, taking the gallery to 74 total sliders (67 hidden, still only 7 public). Planner cleared round-(c) FINAL HANDOFF (0 critical/0 high) after independent verification; published (site version 36, sha matches merge commit); live-verified (public byte-unchanged, admin shows 74/67 hidden correctly, visual spot-check of new entries clean) — didn't re-run the toggle/overlay mechanism tests since the planner confirmed no engine files changed this round, flagged that omission explicitly rather than silently skipping it. POST-RELEASE DONE REPORT sent to planner 2026-08-09. The operator's third ask from the same chat message — a first-class Wixy "posts" content type, before/after extraction that never deletes the source post, eventual daily-sync — is confirmed-deferred future engineering work, not attempted; the archive's metadata shape was deliberately built to match what that entity type will need. Project complete, no open items.

## New task (2026-08-10): contact settings and real Get In Touch

- DONE 00015 [b8k3ve] Settings > Contact tab + real Get In Touch (no more fake form) —
  new `GET /api/admin/global` route + new admin Contact tab (phone/email/address), site
  repo's Contact page form replaced with Call/Email cards. wixy PR #180 (merge
  `04828610a9b47ce3dfd2d18477196a3d904a6a20`), site PR #33 (merge
  `39785a76748a405b79ca3578b2bd8a7e4a7c4443`), published v48, live-verified on BOTH
  ca.cinnamons.uk and the GitHub Pages mirror. See sidecar
  `done/00015-contact-settings-and-real-getintouch-b8k3ve.md` for the full record,
  including a real HIGH-severity bug the planner's FINAL HANDOFF caught before merge
  (the tab wrote phone/email but not their separate phoneHref/emailHref link-target
  keys, which would have silently stranded every real tel:/mailto: link) and a 3rd
  occurrence of the already-documented align-a-photo-pair e2e flake.

## New task (2026-08-09): Before & After editor legibility

- DONE 00008 [thkdzz] Before & After editor legibility overhaul — 3 PRs, all merged+deployed+
  live-verified (PR1 #161, PR2+PR3 #162; decisions/00118, decisions/00119). See sidecar
  `done/00008-before-after-editor-legibility-thkdzz.md` for the full completion record,
  including a real pointer-events bug self-caught in verification and a deliberate deviation
  (skipped the planned real-publish test after finding a genuine pre-existing pending draft
  change on production that wasn't mine to publish — flagged to the planner, left untouched).

## New task (2026-08-10): admin section tabs

- DONE 00013 [fk28mz] Group Before & After's collections into Categories/Photos tabs —
  new generic `AdminCollection.tab` (now Inv 31), wixy PR #174 (merge `22a57b6`),
  merged+deployed+live-verified at mobile width. Planner FINAL HANDOFF (0 critical/0 high),
  pre-scoped conditional clearance after CI first caught a `ruff format` gap in my own
  process (fixed, confirmed mechanical-only, re-verified green). See sidecar
  `done/00013-admin-section-tabs-fk28mz.md` — includes an open UX question for the operator
  (which tab should default-show first) deliberately left unanswered, not decided solo.
- DONE 00014 [qz71vm] Swap tab order: Photos before Categories — resolved the 00013 open
  question (operator chose Photos as default), pure registry reorder, wixy PR #176 (merge
  `5cba2f5`), merged+deployed+live-verified. Also answered two operator questions about
  pre-existing behavior (Remove button's native confirm dialog; dangling-category-value
  behavior) via direct empirical testing, not guessing. See sidecar
  `done/00014-photos-tab-first-qz71vm.md`.

## New task (2026-08-09): category management

- DONE 00012 [h4qz9k] Let Purdi manage Before & After category names herself (rename, add) —
  new generic `optionsFrom` capability on `choice` admin fields + `gallery.categories`
  collection (now Inv 30), 2 wixy PRs (#171 merge `b9b9e35`, #172 merge `6cb9601`) + 1 site PR
  (#31 merge `20910b6`), all merged+deployed+published(v47)+live-verified. Planner ran a
  two-round FINAL HANDOFF gate (0 critical/0 high both rounds). See sidecar
  `done/00012-manageable-gallery-categories-h4qz9k.md` for the full record, including two
  self-caught bugs (a previous handover's unverified "validate passes" claim, and a real
  process gap of my own — committed before finishing verification, fixed+disclosed same
  session) and 3 LOW follow-ups the planner flagged for future work.

## New task (2026-08-09): source-post-link feature

- DONE 00009 [acl02a] Link before/afters back to their original IG/FB post — new `url` admin
  field kind + public link + 74-item backfill, 4 wixy PRs + 1 site PR, all merged+deployed+
  published(v44)+live-verified. See sidecar `done/00009-source-post-link-feature-acl02a.md`
  for the full record, including two real mid-flight discoveries (a registry-kind drift bug,
  and a genuine XSS gap in the href-binding mechanism — both fixed, not deferred) and a
  systemic parity-baseline treadmill flagged as a follow-up needing its own design.
- DONE 00010 [xtxkpk] Production incident: gallery permanently blank — reported live by the
  operator (phone screenshot) right after the v44 publish; root-caused (container-level
  scroll-reveal threshold mathematically unreachable once the gallery grew to 52 items),
  fixed (per-item reveal instead), verified (0/52→52/52 revealed, live + local), and
  confirmed by the operator, all within the same session. Site PR #28 (merge `f97fc49`,
  published v45), decisions/00010 + follow-up correction PR #29 in the site repo. See sidecar
  `done/00010-gallery-blank-incident-xtxkpk.md`. No automated test guards against a
  regression yet — flagged, not started.
- DONE 00011 [b7mqx2] Production bug: vertical swipe on before/after images hijacked page
  scroll — `touch-action:none` on the slider changed to `pan-y`. Site PR #30 (merge
  `b2c5815`), decisions/00011 in the site repo. See sidecar `done/00011-touch-scroll-fix-retro.md`
  (written retroactively alongside the category-management feature below — the fix itself had
  already shipped+verified+operator-confirmed the prior session, only this record was missing).
