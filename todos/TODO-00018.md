# TODO — workspace 00018 (wixy: Purdi's before/after alignment question)

Mission: Purdi added a before/after pair whose photos don't line up ("the lips are in
different positions and I can't see how I can move the image so they match up like the
others"). Build an alignment facility into the Before & After editor she can use herself
— finger-drag + pinch/slider + micro-movement buttons (operator's explicit spec).

- DONE [k7m2x9] Before/after aligner: full-screen canvas dialog (drag, pinch, zoom
  slider, tilt, micro pad, Blend/Split views, Move toggle) that BAKES adjusted photos to
  new uploads through the ordinary media pipeline — no site template/schema changes.
  Registry-driven via `alignAspect` on the collection (Inv 1). Unit + e2e coverage,
  decisions/00109, ANSWERS.md Q-003.
