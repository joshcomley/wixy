# TODO — workspace 00013 (wixy: double-things-need-working-on-arm-armed)

Implementation brief from planning agent (cmd session `5b2083a2-fdf6-4abf-b1b6-bf43ae0975ba`):
publish-corruption fix + calm publish UX, unmissable AI-chat task visibility, dedicated
Before & After editor. Full brief captured in `.scratch/delegate-brief.md` (committed WIP
snapshot) and in this session's original prompt.

- DONE [26c1ux] Workstream 0: repair live draft overlay on ca.cinnamons.uk (discard corrupt
  gallery.sliders op, fix meta.ogImage src), verify, publish. Shipped as live version 20
  (sha 610b482), verified data-cat present on gallery + FAQ in nav, 2026-07-30.
- DONE [cnxxqg] PR 1: editor read-back fixes (attr bindings + nbsp), media contentSrc,
  server write gate (draft_validate.py), self-heal repair endpoint, report endpoint,
  publish-time blank-src schema guard, calm publishDrawer UX. Merged PR #132
  (db1b7b3), 2026-07-31 — full verification green (see sidecar in done/).
- DONE [91ewmd] PR 2: wixy-tasks fenced-block protocol (preamble + extraction + SSE), chat panel
  work banner + task card, conversation list working indicator. Merged PR #133 (0eb35eb),
  2026-07-31 — live-verified; surfaced PR4/PR5 (see 00006/00007).
- DONE [9i5y85] PR 3: registry-configured admin section editor (Before & After), drag-reorder,
  guided add flow, e2e coverage. Merged PR #134 (839d5f4), 2026-07-31 — live-verified (a
  cleanup-script incident during verification was found and fully resolved same session).
- DONE [pr4-00006] PR 4 (new, not in original brief): cmd's `activity` field is an enum, not a
  timestamp — the chat working-indicator was silently dead in production for 5 milestones.
  Merged PR #135 (eec9cb3), 2026-07-31 (decisions/00099). Its OWN live re-verification then
  found it still broken (wrong literal) — see PR 5 below.
- DONE [pr5-00007] PR 5 (new, found live-verifying PR4): PR4's own fix compared against the
  wrong enum literal ("working" instead of the real "active") — still broken after "fixing."
  Merged PR #136 (fa91ccf), 2026-07-31 (decisions/00100). Live-verified CONFIRMED on production:
  a real conversation showed `working:true` via both the dedicated list and `/state`,
  correlated with cmd's own `activity:"active"`. Also flagged, NOT fixed: the standalone/
  anthropic AI backend shares this code path but uses a real timestamp, not this enum
  (decisions/00101 — out of scope, milestone 6 is security-gated).
- DONE [de6z9x] Final live verification of all 5 deliverables (W0/PR1-5) + mandatory completion
  report POSTed to planning agent session 5b2083a2-fdf6-4abf-b1b6-bf43ae0975ba, 2026-07-31.
