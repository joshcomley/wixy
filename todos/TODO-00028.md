# Workspace 00028 tasks

## Open

- 00001 [d5hk92] Execute the search-indexing implementation brief (docs/search-indexing-implementation-brief.md) as a PR train, starting with Work package 0 (baseline) and Work package 1 (staging noindex correction). STATUS as of 2026-08-13 ~07:00: WP1-WP5 all implemented, tested, reviewed, and MERGED to main in both repos (wixy + cottage-aesthetics-preview). WP6 (monitoring/IndexNow) explicitly NOT started per the brief's own words ("assess only after the publish pipeline and URL set stabilize"). **Mission is now blocked on the owner**: decisions/00143 (checked live, not assumed) found that a Slots engine deploy does NOT re-materialize ca.cinnamons.uk's static build (robots.txt/sitemap.xml/every page's rendered head) -- only Purdi's Publish does. So NOTHING shipped this mission (not even WP1's pure-engine noindex fix) is live-verifiable yet, on staging or the public domain. WP5's draft treatment pages will also go live (with their draft-flag banner, not hidden) the moment Publish happens, since they're already merged to site main -- this exact tradeoff (review Purdi's treatment pages before Publish vs. Publish now with draft banners intact) is now owner decision #314, raised by the parent session; standing by for the operator's answer, NOT triggering Publish or starting WP6 in the meantime. The post-Publish WP0-style live-verification pass (robots.txt/sitemap/structured-data/legacy-redirects/fresh Lighthouse median-of-3 against both hosts) is tracked as this workspace's durable follow-up task 00027 [lqj551] by the parent session -- ready to run the moment Publish happens, whenever the operator answers.

## Done

- DONE 00005 [k3n8qz] WP4 (brief table Order 5, mobile performance 4A-4E), fully complete: 4A/4C/4E site-repo (PR #45, merged e1355765), 4B engine (PR #205, merged b2774b1), 4D image-bytes (evidence-gated): a naive first measurement pass wrongly flagged ~9.7 MiB across 111 images -- caught before acting (gallery has a lightbox with 2x zoom my `<img>`-only DOM probe never opened; `lounge.jpg` is also the homepage's CSS `hero.bg`, invisible to that same probe). Real, evidence-backed finding: 4 images genuinely oversized, converging with the brief's own WP0 Lighthouse estimate (~1.13 MiB). Dispatched crop-aware resize targets to the site-repo session; it independently re-verified every claim, resized via Pillow (LANCZOS, quality 85), and reported the precise real result: 815,156 bytes (796 KiB, 64.8%) removed across the 4 files, zero perceptible quality loss (confirmed by pixel-diff + visual inspection). Merged as cottage-aesthetics-preview PR #47 (700146e). Real CI's `--strict-screenshots` (1% budget) found 5 baseline-drift failures where local dev parity only predicted 2 (pixel-dimension fingerprint drift also tripped the screenshot budget, not just the image-set check) -- recaptured via wixy PR #215 (f8a1cf3), same `capture-baseline.yml`-against-the-PR-branch pattern as WP5.

- DONE 00009 [p3f7my] WP5 (brief table Order 6, treatment landing pages): sourced all copy verbatim/paraphrased from existing content/treatments.json, about.json, faq.json, aftercare.json (no invented candidacy/contraindication/outcome claims). Design: 4 new pages (/microneedling, /skin-boosters, /polynucleotides, /dermal-fillers), NOT in top nav — linked contextually from /treatments via a new conditional `detailUrl` field + "Learn more" link. Dispatched full content package to the reused site-repo session e46c4302 (workspace #18); it caught+fixed 2 real bugs (unescaped `&`, absent-vs-empty `detailUrl` crashing the build) and added a draft-flag banner on all 4 pages beyond the original spec. Reviewed content/sourcing/decisions/00022 in full before merging. Merged as cottage-aesthetics-preview PR #46 (3ec7d8b). Required a wixy-side parity baseline recapture first (the /treatments hub links changed its text/link set) — merged as wixy PR #212 (29d7057) via capture-baseline.yml against the PR branch, per decisions/00008's established precedent. Draft only — Purdi's clinical-review checklist (in the PR description) + Wixy preview approval + Publish still required before this is live; not agent-doable.

- DONE 00002 [1oie6p] Unblocked main CI: claude-agent-sdk 0.2.137 added ConversationResetMessage to ClaudeSDKClient.receive_response()'s return type, breaking mypy --strict on wixy_server/worker/agent_client.py. Fixed + regression test, PR #198 merged as d3cb9db.
- DONE 00003 [7pxrv9] WP1 follow-up (flagged by the architecture consult, relation b2834443): X-Robots-Tag noindex middleware. Audit gate was blocked by a real infra fault (poisoned senior-sonnet relations credential); fixed by a separate cmd repair lane (cmd PR #2111, merged d8ce0886). Fresh audit clean at opus (0C/0H). Merged as wixy PR #203 (d0078819).
- DONE 00004 [gy7hb4] WP2-site (legacy redirect map + pages.yml wiring) dispatched to cottage-aesthetics-preview workspace #18 (session e46c4302): merged as cottage-aesthetics-preview PR #43 (c337096).
- DONE 00006 [q7vxht] WP3-site (structured data + favicon, site-repo half) dispatched to the same reused workspace #18 (session e46c4302): business block + favicon.ico/svg/apple-touch-icon.png + <link rel="icon"> tags added, verified against wixy engine PR #204 (8783844), merged as cottage-aesthetics-preview PR #44 (5b7e20d). Favicon still needs Purdi's visual sign-off before Publish (artifact: https://claude.ai/code/artifact/ba89d011-5461-49f3-b6e8-846bc9b19e07).
- DONE 00007 [m4tj2s] WP4-site (4A reveal-gating fix, 4C lazy gallery loading, 4E fonts audit) dispatched to the same workspace #18 (session e46c4302): merged as cottage-aesthetics-preview PR #45 (e1355765). Found + I fixed 3 engine-side parity-harness gaps along the way (wixy PRs #206/#207/#208, decisions/00141+00142): capture never waited for lazy-loaded images, then a decode()-vs-load-event gap, then a stale [0,0] baseline entry unmasked by the first two fixes.
- DONE 00008 [x9wblq] WP3-engine (homepage JSON-LD WebSite+LocalBusiness + favicon passthrough, decisions/00139/Inv 38). Fable consult 152f1a9f ruled the design. Fresh audit (once the gate was restored) found a genuine critical (script-tag breakout XSS in inject_structured_data -- fixed with \uXXXX escaping) plus 2 medium/low address-drift edge cases, all fixed across 3 rounds. Merged as wixy PR #204 (99292b3).
