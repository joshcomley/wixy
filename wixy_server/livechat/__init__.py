"""Server-chat (disguised "Server" admin panel) — spec/server-chat/00-brief.md.

Human<->human admin messaging, private and separate from the AI chat (`chats.py`,
`cmdchat.py`) and the publish-lifecycle media pipeline (`media.py`). See the brief
for the module map; each submodule's own docstring covers its slice.
"""

from __future__ import annotations
