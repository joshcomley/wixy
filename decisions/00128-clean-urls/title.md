Public page URLs drop the `.html` suffix (`/about`, not `/about.html`) with zero redirects
either direction — the server mirrors GitHub Pages' own extensionless resolution instead of
diverging from it; the legacy `.html` shape keeps resolving forever.
