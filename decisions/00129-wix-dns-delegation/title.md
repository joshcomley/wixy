The go-live guide now opens Part 2 by taking the domain's nameservers back from Wix before
touching Name.com's DNS records — the domain was still fully delegated to Wix's own DNS
(measured, not assumed), which would have made every record added at Name.com a no-op.
