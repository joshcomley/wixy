A generic `tab` grouping on `AdminCollection` lets a section split its collections across a
switchable tab strip; the Before & After section uses it to separate "Categories" from
"Photos" instead of stacking all three collections on one long scroll.
