PR2: the "Show on site" toggle moved to a prominent card-header switch with plain wording and collection-wide Turn all on/off buttons, replacing the small "Hidden" chip; Save's success detection now comes from a real per-batch signal the op queue reports, not an inference from `rev` advancing. PR3: each 2-photo card gets an inline drag-to-compare before/after preview with a tap-to-enlarge modal, ported from the public site's own slider.
