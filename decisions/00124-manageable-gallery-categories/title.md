A new `optionsFrom` capability on `choice`-kind admin fields, plus a `gallery.categories`
admin-managed collection, lets Purdi rename/add before-after category labels herself
instead of a developer editing a hardcoded list.
