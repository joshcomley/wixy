# Pinned third-party dependencies for the deployed Wixy engine (spec/07-hosting-deploy.md
# §1). `deploy.py`'s `_pip_install_venv` build step installs this into each slot's fresh
# `.venv` BEFORE installing the `wixy` package itself (`--no-deps`, since every one of its
# runtime deps is already pinned here) — so every deploy resolves the exact same dependency
# graph regardless of what a loose `pip install ".[server]"` might otherwise pick up on a
# different day. Covers both `dependencies` and the `server` extra from `pyproject.toml`
# (deployed slots run `wixy_server`, which imports `builder`, so both are needed).
#
# Regenerate with a clean venv against the pythoncore-3.14 interpreter:
#   python -m venv /tmp/reqs-venv && /tmp/reqs-venv/Scripts/pip install ".[server]"
#   /tmp/reqs-venv/Scripts/pip freeze | findstr /vi "^wixy" > requirements.txt
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.1
beautifulsoup4==4.15.0
certifi==2026.6.17
cffi==2.1.0
click==8.4.2
colorama==0.4.6
cryptography==49.0.0
fastapi==0.139.0
greenlet==3.5.3
h11==0.16.0
html5lib==1.1
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.18
nh3==0.3.6
pillow==12.3.0
playwright==1.61.0
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
pyee==13.0.1
PyJWT==2.13.0
python-dotenv==1.2.2
python-multipart==0.0.32
PyYAML==6.0.3
six==1.17.0
soupsieve==2.8.4
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.16.0
uvicorn==0.51.0
watchfiles==1.2.0
webencodings==0.5.1
websockets==16.1
