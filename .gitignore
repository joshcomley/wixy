# secrets — never commit
.env
.env.local
.env.*.local
secrets/

# Wix CLI per-project state
.wix/

# node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# build output
dist/
build/
.astro/

# python
__pycache__/
*.pyc
*.egg-info/
.mypy_cache/
.pytest_cache/
.ruff_cache/
.venv/

# playwright
/e2e/test-results/
/e2e/playwright-report/
/e2e/blob-report/

# editors / OS
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# handover skill scratch — disposable per-handover continuation docs
handover/

# Uxer framework source (slice 7's MCP compliance-bridge integration) — cloned
# locally for its web/dist/uxer-compliance.js bundle, not committed (mirrors
# Uxer's own integration-guide convention: "Add Uxer/ to your .gitignore so
# the Uxer source is not committed into your repo").
Uxer/
